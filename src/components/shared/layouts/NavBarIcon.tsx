import { MdMenu } from "react-icons/md";

type Props = {};

const NavBarIcon = (props: Props) => {
  return <MdMenu className="swap-off fill-current w-6 h-6 focus:btn-ghost" />;
};

export default NavBarIcon;
